# class SessionsController < ApplicationController

  # def new
  #   if logged_in?
  #   end
  # end
  #
  # def create
  #   user = User.find_by(:email => params[:email])
  #   if user && user.authenticate(params[:password])
  #     render :json => user, :only => [:id, :email]
  #   else
  #     render :json => { errors: "Your login information does not match our records. Please try again." }
  #   end
  # end
  #
  # def destroy
  #   session.clear
  # end

# end